package com.impressdesigns.alex.repositories;

import com.impressdesigns.alex.objects.OrderDesignLocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderDesignLocationRepository extends JpaRepository<OrderDesignLocation, Integer> {
}
