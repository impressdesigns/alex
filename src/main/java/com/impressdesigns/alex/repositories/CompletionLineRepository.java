package com.impressdesigns.alex.repositories;

import com.impressdesigns.alex.objects.CompletionLine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompletionLineRepository extends JpaRepository<CompletionLine, Integer> {
}
