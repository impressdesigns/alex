package com.impressdesigns.alex.repositories;

import com.impressdesigns.alex.objects.OrderDesign;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderDesignRepository extends JpaRepository<OrderDesign, Integer> {
}
